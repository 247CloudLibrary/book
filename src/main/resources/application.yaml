spring:
  profiles:
    active: local
  datasource:
    hikari:
      jdbc-url: cloudlibrary/rds/book/mysql
      driver-class-name: com.amazonaws.secretsmanager.sql.AWSSecretsManagerMySQLDriver
      username: cloudlibrary/rds/book/mysql
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update


feign:
  client:
    config:
      default:
        connectTimout: 100
        readTimeout: 1000
        loggerLevel: HEADERS
  composite-api:
    url: ecs-alb-167470959.us-east-1.elb.amazonaws.com/v1
logging:
  level:
    root: info
    com.cloudlibrary.books: debug

jwt:
  header: Authorization
  secret: usertokensecretcloudlibraryhhjsisywsyjsdkyjfightingkoreausertokensecretcloudlibraryhhjsisywsyjsdkyjfightingkorea
  token-validity-in-seconds: 86400000
---
spring:
  application:
    name: book-sample-local
  config:
    activate:
      on-profile: local

---
spring:
  application:
    name: book-sample-dev
  config:
    activate:
      on-profile: dev

---
spring:
  application:
    name: book-sample-prod
  config:
    activate:
      on-profile: prod